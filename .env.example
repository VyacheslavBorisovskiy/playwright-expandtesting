# Copy to .env. Both default to the public practice site if unset.
BASE_URL=https://practice.expandtesting.com/
API_URL=https://practice.expandtesting.com/notes/api/

# Optional: a pre-existing account. The smoke suite does NOT need these — it
# registers throwaway users per run and deletes them afterwards.
TEST_EMAIL=
TEST_PASSWORD=
